const defaultState = {
    semester: 'Fall 2017'
};

const CourseReducer = (state = defaultState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default CourseReducer;